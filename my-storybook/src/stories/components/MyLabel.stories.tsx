import { MyLabel } from "../../components/MyLabel";

export default {
  title: "UI/Etiquetas/MyLabel",
  component: MyLabel,
};

const Template = () => <MyLabel></MyLabel>;

export const BasicLabel = Template.bind({});
export const SecondaryLabel = Template.bind({});
